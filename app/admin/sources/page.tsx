import { createClient } from "@/lib/supabase/server";
export default async function SourcesPage() { const supabase = await createClient(); const { data, error } = await supabase.from("benchmark_sources").select("*").order("name"); return <main className="main"><div className="hero"><div><h1>Benchmark sources</h1><p className="muted">Maintain the public sources and queries used for monthly research.</p></div></div>{error ? <p className="error">{error.message}</p> : <div className="table-wrap"><table><thead><tr><th>Source</th><th>Type</th><th>Platforms</th><th>Queries</th><th>Verification</th><th>Status</th></tr></thead><tbody>{(data ?? []).map((row) => <tr key={row.id}><td><strong>{row.name}</strong><br /><span className="muted">{row.organization_account || "Account not configured"}</span></td><td><span className="badge">{row.source_type}</span></td><td>{(row.platforms ?? []).join(", ")}</td><td>{(row.search_queries ?? []).join(" · ")}</td><td>{row.verification_status}</td><td>{row.enabled ? "Enabled" : "Disabled"}</td></tr>)}</tbody></table></div>}</main>; }
