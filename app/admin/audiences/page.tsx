import { createClient } from "@/lib/supabase/server";
export default async function AudiencesPage() { const supabase = await createClient(); const { data, error } = await supabase.from("target_audiences").select("*").order("name"); return <main className="main"><div className="hero"><div><h1>Target audiences</h1><p className="muted">Define who each planning cycle should reach.</p></div></div>{error ? <p className="error">{error.message}</p> : <div className="table-wrap"><table><thead><tr><th>Audience</th><th>Priority</th><th>Campaigns</th><th>Platforms</th><th>Content angles</th></tr></thead><tbody>{(data ?? []).map((row) => <tr key={row.id}><td><strong>{row.name}</strong></td><td><span className="badge">{row.priority}</span></td><td>{(row.relevant_campaigns ?? []).join(", ")}</td><td>{(row.preferred_platforms ?? []).join(", ")}</td><td>{(row.content_angles ?? []).join(" · ")}</td></tr>)}</tbody></table></div>}</main>; }
