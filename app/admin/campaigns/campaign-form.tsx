"use client";
import { useState } from "react";
export function CampaignForm() { const [open, setOpen] = useState(false); const [name, setName] = useState(""); const [description, setDescription] = useState(""); const [message, setMessage] = useState(""); async function submit(e: React.FormEvent) { e.preventDefault(); const response = await fetch("/api/campaigns", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ name, description, priority: "medium", target_share: 0, enabled: true }) }); setMessage(response.ok ? "Saved. Refresh to see it." : "Could not save campaign."); } if (!open) return <button className="button primary" onClick={() => setOpen(true)}>Add campaign</button>; return <form className="form" onSubmit={submit} style={{minWidth: 300}}><label>Name<input required value={name} onChange={e => setName(e.target.value)} /></label><label>Description<textarea required value={description} onChange={e => setDescription(e.target.value)} /></label><div className="actions"><button className="button primary">Save</button><button type="button" className="button" onClick={() => setOpen(false)}>Cancel</button></div>{message && <span className="muted">{message}</span>}</form>; }
