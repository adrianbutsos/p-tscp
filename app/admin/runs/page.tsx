import { createClient } from "@/lib/supabase/server";
import { RunButton } from "./run-button";
export default async function RunsPage() { const supabase = await createClient(); const { data, error } = await supabase.from("benchmark_runs").select("*").order("created_at", { ascending: false }).limit(20); return <main className="main"><div className="hero"><div><h1>Benchmark runs</h1><p className="muted">Start a monthly or on-demand benchmark request and track its status.</p></div><RunButton /></div><div className="notice">The benchmark endpoint requires <code>OPENAI_API_KEY</code> in the server environment. It never exposes that key to the browser.</div><div className="table-wrap" style={{marginTop: "1rem"}}><table><thead><tr><th>Planning period</th><th>Trigger</th><th>Status</th><th>Created</th><th>Error</th></tr></thead><tbody>{error ? <tr><td colSpan={5}>{error.message}</td></tr> : (data ?? []).map((row) => <tr key={row.id}><td>{row.period_start} → {row.period_end}</td><td>{row.trigger}</td><td><span className="badge">{row.status}</span></td><td>{new Date(row.created_at).toLocaleString()}</td><td>{row.error_message || "—"}</td></tr>)}</tbody></table></div></main>; }
